apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "some-app2.fullname" . }}-test-connection"
  labels:
    {{- include "some-app2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "some-app2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
